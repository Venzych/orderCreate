### Входные параметры:

1. **rstMeterReq** – Набор данных заявки на замер (Recordset).
2. **lngUserID** – Идентификатор пользователя (Long), от имени которого создаётся заказ.
3. **lngProfileID** – Идентификатор профиля (Long), для которого создаётся заказ.
4. **lngTradeAgentID** – Идентификатор торгового агента (Long), для начисления бонусов.
5. **strDCardNum** – Номер дисконтной карты (String).

---

### Локальные переменные:

1. **strReturnString** – Строка для возвращаемых сообщений или результатов (String).
2. **lngOrderID** – Идентификатор созданного заказа (Long).
3. **lngPayCondID** – Идентификатор условий оплаты (Long).
4. **dblExchange** – Курс валюты (Double).
5. **strCommandNumber** – Номер команды или документа (String).
6. **lngCurrencyID** – Идентификатор валюты (Long).
7. **strMetReqNum** – Номер заявки на замер (String).
8. **lngMeterID** – Идентификатор замерного устройства (Long).
9. **lngDocFormID** – Идентификатор формы документа (Long).
10. **lngManagerID** – Идентификатор менеджера (Long).
11. **lngOfficeID** – Идентификатор офиса (Long).
12. **lngDepartID** – Идентификатор отдела (Long).
13. **lngSellerID** – Идентификатор продавца (Long).
14. **lngCarrierID** – Идентификатор перевозчика (Long).
15. **lngMounterID** – Идентификатор монтажника (Long).
16. **lngMakerID** – Идентификатор производителя (Long).
17. **lngWinMakerID** – Идентификатор производителя окон (Long).
18. **lngGlassMakerID** – Идентификатор производителя стекла (Long).
19. **lngAEMakerID** – Идентификатор производителя фурнитуры (Long).
20. **lngTradeMarkID** – Идентификатор торговой марки (Long).
21. **rc** – Код возврата из функции (Long).
22. **rstOrder** – Набор данных для работы с заказом (Recordset).
23. **rstProps** – Набор данных атрибутов заказа (Recordset).
24. **rstProfile** – Набор данных профиля пользователя (Recordset).
25. **objOrdMileSt** – Объект для работы с этапами заказа (clsOrdMileSt).
26. **rstOrdMilest** – Набор данных для этапов заказа (Recordset).
27. **objCurrency** – Объект для работы с валютой (clsCCurrency).
28. **objCRate** – Объект для работы с курсами валют (clsCRate).
29. **objCodif** – Объект для работы с классификаторами (clsCodificator).
30. **rstCRate** – Набор данных курсов валют (Recordset).
31. **objDealerData** – Объект для работы с данными дилеров (clsDealer).
32. **rstDealerData** – Набор данных дилеров (Recordset).
33. **objMetReq** – Объект для работы с заявками на замер (clsMetering).
34. **colMSDates** – Коллекция дат этапов (Collection).
35. **rstVendor** – Набор данных поставщиков (Recordset).
36. **rstMaker** – Набор данных производителей (Recordset).
37. **objUser** – Объект для работы с данными пользователя (clsUser).
38. **objProp** – Объект для работы со свойствами заказа (clsOrderProp).
39. **objFcdOrder** – Объект для работы с функционалом заказов (fcdOrder).
40. **lngMoveConstrRuleID** – Идентификатор правила перемещения конструкций (Long).
41. **lngStockID** – Идентификатор склада (Long).
42. **lngShipWayID** – Идентификатор способа доставки (Long).
43. **lngContrTypeID** – Идентификатор типа договора (Long).
44. **lngContrTypeID1** – Вспомогательный идентификатор типа договора (Long).
45. **lngOtmp** – Временный идентификатор офиса (Long).
46. **strOnum** – Номер офиса (String).
47. **strOnumA** – Альтернативный номер офиса (String).
48. **fIsChangeDF** – Флаг изменения формы документа (Boolean).
49. **strComment** – Комментарий к заказу (String).
50. **n1** – Позиция в строке (Long).

---

### Общие переменные:

1. **objBCData** – Объект с данными бизнес-логики (глобальный объект).
2. **TRATE_EUR** – Константа для обозначения курса евро.
3. **TRATE_RUR** – Константа для обозначения курса рубля.
4. **RSProcLib** – Библиотека функций для работы с наборами данных (глобальная).
5. **DBProcLib** – Библиотека функций для работы с базой данных (глобальная).
